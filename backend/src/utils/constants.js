/** Shared limits and form field names used by upload + validation. */
export const MAX_RESUME_BYTES = 5 * 1024 * 1024

/** Must match the field name your frontend sends in FormData (e.g. `resume`). */
export const RESUME_FIELD_NAME = 'resume'
